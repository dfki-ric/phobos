# 1. we need to fix the $phobostype/property syntax to only annotations in custom properties (all other will be directly editable in the config section of the phobostype)
# only link objects have this as the contain the information for the joint as well
# 2. mimic_* has been renamed to mimic/*
# 3. Make Materials use Specular BSDF
